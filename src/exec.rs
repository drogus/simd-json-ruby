use rutie::{Class, Object, RString, VM, Boolean, Hash, NilClass, Fixnum, Float, Array, AnyObject};

mod implementation;
use crate::implementation::rust_parse;
use crate::implementation::foo;

fn main() {
    println!("FOOO");
    let s = RString::new_utf8("");
}
